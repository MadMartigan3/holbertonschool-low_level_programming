#include "main.h"

/**
 * print_rev - funciton that prints a string in reverse
 *
 *
*/

void print_rev(char *s)
{
	int x = _strlen(s);

	for (x = x - 1; s[x] != '\0'; x--)
	{
		_putchar(s[x]);
	}
	_putchar('\n');
}
